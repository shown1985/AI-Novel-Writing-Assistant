# 当前阶段开发任务：收尾与纠偏

更新时间：2026-09-25
当前分支：`claude/confident-cori-w3ctnw`
开发基线：`main@a580a27`
阶段状态：收尾与纠偏 —— 主线正文生产链（P0/P1/P1.1/执行链清理 Phase 1–5）与 WGR-001～023 的代码均已合入 `main`，尚未经过构建验证与用户验收。

## 工作纪律（先读 `docs/checkpoints/codex-progress-audit-2026-09-24.md` 第 4 节）

1. `TASK.md` 是唯一事实源，WGR 台账（`docs/plans/github-issue-expectations.md`）只记录条目细节。
2. 同一时间只保留一个进行中条目；条目完成或明确搁置后才能开始下一个。
3. 进入“待用户验收”的条目必须在 `docs/checkpoints/user-acceptance-checklist.md` 有对应操作步骤，且该清单同一时间不超过 3 条待验收。
4. 真实运行中发现的新问题先判断是否阻塞当前阶段：阻塞才在当前分支修复，不阻塞写入“待排期”，不开分支不写代码。
5. `server/.backups/` 不得修改、删除或提交。

## 世界生成与模型接入稳定化（WGR）

WGR-001～023（2026-09-05～09-06 期间的世界观生成与模型接入修复）已全部合入 `main`，个人仓库对应 PR 均已关闭并注明合入事实。台账细节见 `docs/plans/github-issue-expectations.md`。按用户可感知效果合并为以下 4 项，状态统一为**已合入 main，未验收**：

| 验收项 | 覆盖 WGR 编号 | 对应验收清单条目 |
|---|---|---|
| 快速配置模型供应商后，新任务立即使用该模型，不回退旧厂商 | WGR-022、023 | `user-acceptance-checklist.md` A 组 |
| 中等规模世界观一键生成成功；失败后可在世界生成页面从最近阶段继续，不丢已完成阶段 | WGR-001、003～007、009～015、017、019、020 | `user-acceptance-checklist.md` B 组 |
| 请求超出模型限制或推理额度耗尽时，界面给出可理解的下一步提示 | WGR-002、008、018 | `user-acceptance-checklist.md` C 组 |
| Mac 安装包首次启动、重启后数据保留、Dock 再激活可用 | WGR-016、021 | `user-acceptance-checklist.md` D 组 |

Issue #126（模型隐藏与思考深度）已随桌面 0.4.18 发布（提交 `3ee10ac`），同样是已合入未验收，验收范围见台账原文，不并入上表。

## 本轮收尾已完成

- 已关闭个人仓库 PR #2～#12、#14～#22，逐条注明内容已随 `a580a27` 进入 `main`；23 个远程功能分支已核实无独有内容，等待用户本机删除（见“当前执行顺序”第 3 步）。
- 新增容器化热更开发环境（`pnpm docker:dev`），已在云端实测冷启动、源码热更和数据卷持久化，详见 `docs/wiki/workflows/docker-dev-environment.md`。
- `server/src/prompting/prompts/world/world.prompts.ts`（曾达 1315 行）已按生成阶段拆分为 facade 加 `prompts/world/stages/` 下的子模块，编译产物逐字节一致。

## 主线正文生产链：已完成任务摘要

以下工作均已合入 `main`，按当时阶段分组，仅保留结论，不保留逐提交细节（历史提交哈希见 Git log）。

- **A. 前置产品任务**：热门题材雷达榜单勾选与全选、低边框视觉规范落地、自动导演章节数可清空重填、简易/专业模式入口优化、专业创作导出 TXT。
- **B. 问题治理预设与运行边界**：“连续完成优先 / 质量优先”实现为问题管理预设而非两套执行器；章节运行与质量修复共用同一重试预算（自动修复最多 1 次）；问题发现阶段改为只读。
- **C. 正文修复与生命周期收束**：手工修复与自动修复共用正文终结服务；时间线检查点写入成功后才释放后续状态；新增章节生命周期服务统一管理 `Chapter.content`、生成状态与章节状态写入。
- **D. 人工审核与旁路收束**：人工审核不再静默调用规划器，只返回结构化质量评估；质量标记、修复历史与章节状态由生命周期服务一次提交；显式重规划、局部窗口调整、数据安全失败之外的旁路已清理。
- **E. 热门题材雷达创作基础收束**：AI 市场分析输出结构化题材基底与推进模式；雷达推荐复用统一资产库，缺失资产由 AI 结构化候选补充。
- **F–G. 真实章节故障闭环**：定位并修复路线窗口同步、问题治理控制流、人工恢复状态回写、JIT 预取失败等真实运行故障；统一任务主状态投影，补齐余额不足（HTTP 402）恢复语义。
- **执行链清理 Phase 1–5**：删除无生产调用的旧 Timeline gate、旧全书重规划循环、`forceFullRewrite` 隐式升级、旧质量循环预算、按修复次数/标签暂停整本任务的风险阈值；命令领取统一收口到 `DirectorCommandLeaseService`，`pendingManualRecovery` 任务不会被后台租约重新拉起。各阶段均有服务端构建通过与定向回归通过的记录（详见历史提交与已归档验证记录）。
- **P0 收束**：人工审核 → 重规划入口收窄为仅在治理动作明确为 `replan`/`stop_for_replan` 时触发；补齐两套预设的端到端回归与恢复矩阵（Worker 重启、租约过期、模型暂不可用、正文保存失败、明确重规划、局部质量债）；统一任务中心、AI 驾驶舱、简易模式、专业模式的状态投影。
- **P1 收束**：补齐旧任务治理快照兼容读取；清理旧风险阈值接线与无调用方代码；`reportIssue` 调用点统一在治理动作执行成功后才记录为已处理；为降级完成的章节补齐质量债来源与修复次数查询。
- **P1.1 收束**：删除恒为 `false/null` 的整章升级归因；停止生产旧 `chapter_rewrite/window_replan` 质量预算动作（历史 ledger 只读兼容）；手动修复后的审校与缓存刷新汇入唯一章节质量闭环。

已完成验证记录：共享契约与服务端构建通过、客户端类型检查通过；章节运行/问题治理/自动导演/状态投影相关定向回归累计数百项通过（各阶段留有独立记录）；未运行浏览器、截图或视觉验收，按项目规则留给用户。**这些验证均在 `main@a580a27` 分叉前完成，尚未在当前 `main` 上重新跑过一次完整回归**，见下方“当前执行顺序”第 1 步。

## 剩余开发任务：P2 集成与真实链路验收

- [x] 问题治理生命周期快照已合并到 `beta` 并完成构建、类型检查和组合回归（09-06 之前）。
- [ ] **P2-1**：`main@a580a27` 上重新跑一次构建、类型检查与组合回归（`beta` 与 `main` 在 09-06 合并时未经过此步）。
- [ ] **P2-2**：使用脱敏真实小说连续运行至少 10 章，验证局部质量债不中断整本生产、恢复不重复写章、明确重规划与数据安全问题仍会暂停（对应验收清单 E 组）。
- [ ] **P2-3**：`beta` 稳定后再决定是否进入 `main`（`main` 事实上已在 09-06 合并，此步是补做的复核，不是重新发起合并）。

## 当前执行顺序

1. **构建 / 类型检查 / 测试验证（进行中）**：在干净的 `main` 上运行 `pnpm install`、`pnpm build`、`pnpm typecheck` 及世界生成、LLM、导演运行时、章节执行相关定向测试；记录结果，不跳过失败项。
2. **修复两个已知既有测试失败**：`prompting-governance.test.js`（`server/src/services/comic/ComicFactService.ts:39/48` 内联 `SystemMessage`/`HumanMessage` 不在 Prompt 治理批准名单，需迁移到 `server/src/prompting/`）；`worldContextGateway.test.js`（调用参数多出 `openingOnly: undefined`）。同时规划（不必本轮执行）`server/src/prompting/core/promptRunner.ts`（1299 行）的拆分，先写职责清单再动代码。
3. **用户本机删除 23 个远程分支**：`codex/*` 15 个、`fix/*` 7 个、`mac` 1 个；云端会话无权限执行删除，需用户在本机操作，删除前可用 `git cherry -v origin/main origin/<branch>` 复核。
4. **用户按验收清单验收**：推荐用 `pnpm docker:dev` 启动（见 `docs/wiki/workflows/docker-dev-environment.md`），对照 `docs/checkpoints/user-acceptance-checklist.md` 逐项验收；升级前先按清单开头的备份步骤备份数据库。
5. **P2-2 真实 10 章运行**：用户本机执行（云端环境没有任何模型 API Key，无法在云端跑真实模型）。
6. **`beta` 组合验证后再决定 `main`**：完成第 1、2 步后在 `beta` 上重跑一次组合回归，再由用户确认是否维持已合入 `main` 的现状。

## 明确不做

- 不增加第三套问题治理路线，不把两套预设复制成两套执行器。
- 不增加修复重试次数或隐藏 LLM 调用，不为尚未出现的旧数据问题预建迁移框架。
- 不在本阶段扩展新的 UI、创作能力或第三方依赖。
- 在“系统完成”六条判定标准（`docs/checkpoints/codex-progress-audit-2026-09-24.md` 第 6.2 节）全部满足之前，不开新的创作能力（漫画、短剧、拆书扩展等计划保持搁置）。
- 在上述六条满足之前，不为新的模型供应商开专门条目；新发现的问题按“工作纪律”第 4 条的范围闸门处理。

## 当前工作区说明

- `server/.backups/` 是未跟踪备份目录；不得修改、删除、提交或纳入工程快照。
- 云端会话没有任何模型 API Key；真实模型调用（世界生成回放、10 章连续生产、桌面首次配置验收）只能由用户在本机执行。
