# 小说生成架构可靠性与扩展能力改进计划

日期：2026-09-07

状态：阶段 D 已完成。B1 验收缓存、B2a 章节流水线持久预算、B2b 导演租约恢复接入、B2c 跨进程流水线执行所有权与底层重试边界，以及 C、D 的章节完成边界均已实现并完成聚焦验证。A.1 复审遗留两项测试缺口由用户明确暂缓；本文不授权自动推送、合并或发布。

审查基线：`beta`，`25db3223`。实施前须核对当前分支、相关源码及后续提交，避免修复已解决的问题。

## 1. 目标与边界

面向不懂小说创作的新手，优先保证整本小说连续完成，同时降低后续增加创作能力的开发成本。

本计划解决三个问题：

1. 章节生成、验收、修复与事实提交在失败和恢复时保持一致，减少重复调用和修复倒退。
2. 新能力可以通过明确的生产步骤或上下文提供者接入，减少跨层修改。
3. 用小规模、可复用的验证样本证明改善，控制开发会话额度与产品运行时模型费用。

保留现有统一章节执行入口、Prompt Registry、问题策略快照、即时章节规划、局部修复和质量债机制。暂不整体重写导演系统，不引入通用工作流平台、微服务或新的创作模式。

本轮审查未运行真实整本生成，也未读取用户小说数据库；不能据此断言实际小说质量或线上故障频率。

## 2. 证据与待验证事项

审查使用 CodeGraph 定位依赖，精读核心实现，并在内存中转译当前 TypeScript 源码，以模拟依赖执行隔离验证。没有使用旧 dist 作为验证依据，没有调用数据库或模型。隔离脚本未保存为仓库回归测试，阶段 A 须将场景固化。

| 编号 | 观察 | 证据级别 | 主要入口 |
| --- | --- | --- | --- |
| R1 | 同正文、同模型下修改规划或目标字数，验收仍复用旧结果；缓存身份未包含完整验收依赖 | 隔离验证确认 | `runtime/ChapterQualityGateService.ts` |
| R2 | 临时 `acceptance_gate_unavailable` 不写持久缓存，但仍留在实例内存缓存 | 隔离验证确认 | `runtime/ChapterQualityGateService.ts` |
| R3 | 修复调用抛普通异常时，调用后面的预算扣减未执行 | 隔离验证确认 | `runtime/chapterRuntimePipeline.ts` |
| R4 | 资产检查点为 running 时直接返回，且可能被本地记录为已同步；抽取失败被捕获后正常返回 | 两项隔离验证确认 | `runtime/ChapterArtifactBackgroundSyncService.ts` |
| R5 | 模拟复审分数从 70 降到 20，流水线仍保存并同步修后稿，没有选择原稿的步骤 | 隔离验证确认；不代表真实模型必然降低质量 | `runtime/chapterRuntimePipeline.ts` |
| R6 | approved 写入早于最终资产同步，外层执行器会按完成状态跳过章节 | 代码确认顺序；需故障注入验证完整恢复后果 | `runtime/chapterRuntimePipeline.ts`、`production/NovelPipelineExecutor.ts` |
| R7 | 每章重试消耗保存在执行局部变量；任务总次数不能直接说明该章消耗 | 代码确认；需验证 Worker 恢复路径 | `production/NovelPipelineExecutor.ts` |
| R8 | 统一资产抽取后，多类资产分别写入，存在部分应用的中断边界 | 代码确认；需逐个检查消费者幂等性 | `runtime/ChapterArtifactDeltaService.ts` |
| R9 | 部分导演步骤在自动模式下只检查事实，实际工作嵌套于章节循环 | 架构事实 | `director/workflowStepRuntime/directorExecutionStepModules.ts` |
| R10 | 上下文组装同时触发规划写入，并集中依赖多种业务服务 | 架构事实 | `runtime/GenerationContextAssembler.ts` |
| R11 | 批次缓存使用进程内事件失效与 30 分钟 TTL | 实现事实；跨进程过期风险待验证 | `runtime/BatchContextCache.ts`、`server/src/events/EventBus.ts` |
| R12 | 角色时间线仍按姓名匹配句子提取，并进入检索索引 | 代码确认；误归属比例未测量 | `runtime/ChapterArtifactSyncService.ts` |

上述 `runtime/`、`production/`、`director/` 相对 `server/src/services/novel/`。

## 3. 目标执行合同

```text
准备本章规划 → 读取版本化上下文 → 生成并保存工作稿 → 验收
                                                    ↓
                               可选一次修复 → 复审 → 选择保留稿
                                                    ↓
                           时间线终态 + 必需连续性资产提交
                                                    ↓
                         记录质量债 / 策略暂停 / 释放下一章
```

约束：

- 正文可读、质量通过、事实已提交是不同事实，不能只用一个 completed 表达全部状态。
- completion-first 下，普通质量债不阻断全书；quality-first 的显式 `pause_for_manual` 必须停在已保存边界，等待用户恢复。
- 明确重规划、无可用正文、数据完整性和受保护内容冲突继续使用安全停止规则。
- AI 负责创作语义和结构化问题理解；确定性运行时负责校验、预算、幂等、持久化及执行已解析的策略。
- 事实同步失败可以按明确规则降级，但必须返回降级证据，不能伪装为同步成功。
- 保留稿变更后，所有派生产物须能关联到同一正文版本；旧版本结果不得覆盖新版本。

## 4. 分阶段实施

### 阶段 A：固化证据与低成本回归基线

目标：将本次观察转为可重复的行为测试，防止后续按印象重构。

- [x] 固化 R1–R5 的六项隔离场景，使用真实业务函数和模拟外部依赖。
- [x] 增加中断位置：修复调用前后、正文保存后、approved 后、资产部分提交后。部分资产应用仅模拟协调器边界，真实消费者幂等性仍待 C3 验证。
- [x] 核对现有测试：已有章节流水线测试依赖 dist；新增 `server/tests/novelProduction/` 专属源码隔离套件，避免扩大原测试文件或重复全量构建。
- [x] 记录现有正常链路模型调用阶段、缓存命中、修复次数、章节推进结果。
- [x] 为待修缺陷保留明确记录：A.1 使用带 KNOWN 标识的普通特征测试精确断言当前缺陷，意外异常必须失败；修复时替换为目标行为断言。

验收：全部场景可在无 API Key、无真实数据库写入条件下运行；报告区分已复现、未复现和未覆盖。

阶段 A 验证记录：

以下为初始历史结果，已由 A.1 结果替代，不能用作当前验收状态。

- 命令：`node --test --test-reporter=spec server/tests/novelProduction/reliabilityBaseline.test.js`。
- 结果：17 项，10 项通过，7 项已知缺陷 TODO（均执行并发生目标断言失败），0 项非预期失败。
- 确认 R1、R2、R3、R4a、R4b、R5，并增加 R6 的 approved 后同步失败场景；退出码 0 不表示这些缺陷已修复。
- 正常生成：writer 1 次、acceptance 1 次、repair 0 次、最终同步入口 1 次；正常修复：repair 1 次、acceptance 2 次、最终同步入口 1 次。以上为模拟依赖调用次数，不包含模型 SDK 内部重试，也不是生产耗时或 token 数据。
- 同输入内存及持久缓存复用确认；临时失败不持久化确认；正文保存后异常仍保留模拟持久正文；修后复审异常前预算已扣减。
- R7 的真实 Worker 恢复、R8 的真实消费者幂等性、跨进程缓存与真实模型质量尚未验证。
- 本阶段未修改运行时或数据库结构；未运行全量构建、浏览器或真实模型评测。发布说明因纯内部测试与文档改动而跳过。
- 长期测试边界记录于 [章节可靠性隔离验证](../wiki/debugging/chapter-reliability-isolated-tests.md)。

### 阶段 A.1：测试闭环补充

- [x] 移除宽泛 TODO，六项 R1–R5 场景使用 KNOWN 特征断言；生产缺陷修复后须更新为目标合同。
- [x] 区分执行前取消与修复阶段取消；新增精确复现：进入 repairing 时取消仍执行 repair/save，列为后续运行时取消边界问题。
- [x] 真实检查点函数配合持久层替身，验证 running 领取、部分失败标记 failed、新实例重新领取、成功写 succeeded、成功结果跳过抽取。
- [x] R6 改为同步异常向上传递和单章直接重入补齐同步，不强制 approved 回退。
- [x] 内存注入意外 TypeError 与错误断言，两个子进程均退出 1；未改动源码进行故障注入。
- [x] 聚焦套件验证：`node --test --test-reporter=tap server/tests/novelProduction/*.test.js`，20 项通过、0 失败、0 TODO、0 跳过。

结果解释：20 项通过包含七项 KNOWN 缺陷复现，不能表述为生产缺陷已修复。R6 外层任务过滤/Worker 恢复、R7 持久预算、R8 真实消费者幂等性仍待后续阶段。测试基础设施与文档已闭合至上述明确范围；未运行全量构建或收费模型调用。

新增取消问题在阶段 B 预算领取边界核对：调用模型前应重新检查取消状态，避免在验收期间收到取消后仍发起修复。

### 阶段 B：验收缓存与自动处理预算

范围：R1、R2、R3、R7。

- [x] B1 定义验收输入指纹：正文 hash、实际渲染的验收上下文、Prompt 版本及槽位/模板内容、有效模型配置和参数。
- [x] B1 指纹使用稳定序列化，不包含运行包时间戳等无关波动；旧缓存作为 miss 处理，无需清库。
- [x] B1 临时系统失败不进入内存或持久成功缓存；Promise 失败或返回不可复用结果时释放相应缓存项。
- [x] 核对实例内与跨 Worker 的并发请求：同实例相同验收输入合并；自动章节流水线由持久作业租约跨 Worker 排他，不在验收缓存内叠加第二套分布式锁。
- [x] 建立章节尝试预约，模型调用前按 job + chapter 原子领取自动处理预算并记录原因；流水线执行身份由作业租约独立持有。
- [x] 恢复时复用任务策略快照和尝试记录。区分主动的新修复任务与旧任务恢复，不静默刷新预算。
- [x] 核对低层网络重试、结构修复重试与业务重试的关系：关闭 OpenAI 兼容客户端隐式重试；结构修复和业务重试保留为显式调用，并分别累计供应商返回的 usage。

验收：同输入复用、相关输入变更失效、临时失败可重试；并发和恢复不超出业务自动处理预算；安全错误不自动重写正文。

B1 范围与验证：R1、R2 已转为目标行为测试，补充并发 miss、调用异常、配置变化、配置读取失败和旧缓存兼容场景。模型执行前后核对身份；不可确认身份时仍允许正常验收，但不复用缓存。同实例在途合并由验收服务负责；自动章节流水线的跨实例/Worker 排他由 B2c 作业租约负责。手动入口或不同作业之间不宣称全局 single-flight。

B1 验证结果：`node --test --test-reporter=tap server/tests/novelProduction/*.test.js` 共 28 项通过、0 TODO；其余 KNOWN 项仍为待修缺陷复现。服务端 `node node_modules/typescript/bin/tsc -p tsconfig.json --noEmit` 通过。首次类型检查受旧 Prisma Client 缺少 authMode 字段影响，执行 `prisma generate --config prisma.config.ts` 后通过；未运行迁移或访问小说数据。未运行整本生成、浏览器和跨进程集成测试。

A.1 暂缓事项：R4b 未断言抽取确实调用；资产检查点替身未匹配完整复合身份。这两项按用户要求暂缓，不阻止阶段 B，不能标为已修复。B1 新增缓存替身按完整 where 身份存储，不扩大修改暂缓的资产测试。

B2a 交付：新增 ChapterAutomaticAttempt 的 PostgreSQL/SQLite 增量迁移与 Prisma 合同，jobId + chapterId 唯一预约保证同 job 重入不重置额度；质量修复与运行异常重试调用前领取，修复前及预约后检查取消。R3 与取消场景已从 KNOWN 改为目标断言。记录表示保守预约，不表示模型成功完成；新 job 独立预算，旧 job 无逐章记录按未预约兼容，不猜测历史消耗。

B2a 验证：32 项源码隔离测试通过；服务端 `tsc -p server/tsconfig.json --noEmit` 通过。新增内存 SQLite 实际执行迁移与唯一约束，验证服务实例重建、重复领取、任务/章节隔离、外键拒绝。无用户数据库写入；Prisma Client 已重新生成。该测试不等同于真实多进程 Worker 或 PostgreSQL 迁移验收。

B2b 交付：导演命令租约次数与章节自动处理预算解除绑定。任务保存的流水线作业仍可继续时，过期导演命令仅重新挂接原作业；作业或任务存在人工恢复标记时保持并向任务传播暂停，后台扫描不再清除。恢复状态无法验证时按数据完整性风险暂停。尚未进入章节执行的命令继续使用原导演问题治理，不伪造章节身份。

B2b 验证：导演租约聚焦测试 6 项通过，覆盖原作业挂接、已耗尽 command attempt、流水线人工暂停、任务人工暂停和恢复状态读取失败；小说生产隔离套件 35 项通过，新增验证旧预约重入时单章自动额度为 0、修复已领取后异常不触发外层重试、预约读取失败时不进入章节模型链。服务端 TypeScript 构建通过。未运行收费模型、浏览器、用户数据库迁移或 PostgreSQL 迁移。

B2c 交付：`GenerationJob` 持久保存执行 owner 与租约到期时间。调度器原子领取后才进入章节执行，运行期间定时续租；旧执行者在模型入口、章节边界或状态写回时发现 owner 变化或租约过期即静默退出，不能覆盖新执行者终态。人工暂停、取消和非活动作业不能领取。租约存储不可用时，任务在任何模型调用前转入人工恢复，不使用进程内集合掩盖迁移或连接问题。

B2c 调用计量合同：OpenAI 兼容客户端的 SDK 隐式网络重试设为 0；业务重试、结构化策略切换和 JSON 修复仍是可观察的显式模型调用。现有 usage tracking 会为每个成功返回 token usage 的显式 invoke/stream 分别累计调用次数和 token。网络失败且供应商没有返回 usage 时，系统无法判断上游是否已计费，因此只保留业务尝试与错误日志，不承诺物理请求数或计费次数绝对精确。

B2c 验证：服务端 TypeScript 构建通过；小说生产套件 38 项、流水线状态 9 项、运行恢复 4 项、导演陈旧租约 6 项、LLM 重试策略 1 项、usage 提取与流式合并 4 项通过。内存 SQLite 实际执行新增迁移并验证竞争领取仅一方成功、过期接管、过期 owner 不可续租、非 owner 不可释放、人工暂停不可领取及索引存在；执行器验证旧 owner 不进入章节模型且不写失败终态。未运行收费模型、浏览器、用户数据库迁移或 PostgreSQL 迁移；PostgreSQL 迁移仍需在预发布环境按正常发布流程验证。

B2 闭合复审修复：owner 的进度与成功/失败写回增加运行态、人工暂停和取消 CAS，取消收尾使用独立条件；导演任务显式关联作业不存在或身份不匹配时按数据完整性风险暂停；租约续期只允许到期时间向后推进；租约字段缺失时通过不返回完整模型的旧字段兼容更新保存人工恢复状态，其他领取异常不得暂停仍有有效 owner 的作业。

B2 闭合复审验证：服务端 TypeScript 构建通过；小说生产套件 41 项、流水线状态 9 项、运行恢复 4 项、导演陈旧租约 7 项、LLM 重试策略 1 项通过。新增覆盖章节返回后并发取消不能被成功终态覆盖、有效 owner 仍可正常完成、迟到续租不能缩短租约、缺列兼容暂停走真实存储适配器、非缺列异常保留有效 owner、显式关联作业丢失进入人工恢复。未运行收费模型、浏览器、用户数据库迁移或 PostgreSQL 迁移；A.1 两项测试缺口继续按用户要求暂缓。

### 阶段 C：保留稿选择与连续性提交边界

范围：R4、R5、R6、R8。按 C1、C2、C3 分成独立可验证提交。

#### C1：修复候选与版本选择

- [x] 保存原稿与修复候选的关联；优先复用现有版本能力，避免新建第二套正文版本系统。
- [x] 修后复审完成再选择保留稿，保持“最多一次自动修文”。
- [x] 复用已有两次验收的结构化结果，关注严重问题、义务覆盖、禁止越界和已成立内容；总分仅作辅助。
- [x] 证据不足或候选新增严重问题时保留原稿，并记录修复未采用的原因。
- [x] 全部终态资产只针对选定版本提交，候选写作期间不将其当作正史。

验收：修复倒退可保留原稿；局部改善可采用候选；修后验收失败不会丢失原稿；不额外默认添加一次模型评选。

C1 实施记录：候选在本次章节运行内与原稿配对比较，不新增章节版本表；长期恢复继续使用流水线启动前的 `NovelSnapshot`。选择器只消费两次既有验收的通过状态、严重问题、阻塞问题和义务缺口，总分只写入选择记录作诊断。候选复审完成前不覆盖 `Chapter.content`，选定后才保存正文，并且只对选中版本执行时间线、事实、终态事件和统一资产同步。选择结果以正文 hash、分数和原因写入质量闭环，不在风险标记中复制正文。

C1 验证：服务端 TypeScript 构建通过；候选选择、章节流水线、终态提交、质量记录和可靠性定向测试 71 项通过；小说生产、流水线状态与运行恢复回归 54 项通过。覆盖恶化候选保留原稿、候选通过后采用、严重问题/义务退化拒绝、只提高总分不采用、复审异常不丢原稿、两次验收不增加模型评选、终态只提交一次。未运行收费模型、浏览器、数据库迁移或用户数据写入；A.1 两项遗留继续按用户要求暂缓。

#### C2：区分同步结果与完成状态

- [x] 把同步返回值改为可判别结果，覆盖 completed、pending、degraded、failed，携带正文版本和已完成产物。
- [x] 同版本任务正在运行时等待其完成，或显式返回 pending；pending 不写入本地成功缓存。
- [x] approved/完成投影与“可进入下一章”分别处理，恢复以当前版本的产物检查点为依据。
- [x] 抽取失败时按事实完整性选择受控重试、最小连续性降级或安全暂停；普通质量债不得被误升级为全局停止。
- [x] 关闭自动审核的路径仍要满足连续性提交边界，不伪造审核通过证据。

验收：下一章只在必需资产完成或明确降级后启动；每个中断点都能补齐缺失产物；不能因旧 approved 状态跳过必要收尾。

C2 实施记录：章节同步统一返回 `completed / pending / degraded / failed`，并携带正文 hash 与已完成产物。进程内相同正文版本复用同一个执行 Promise，只有持久检查点确认完成的结果才进入成功缓存；跨进程运行中检查点显式返回 `pending`。章节终态在最终保留稿的 Timeline 和资产同步边界之后发布，`pending / failed` 保留可恢复边界，`degraded` 作为明确记录的连续性降级允许继续，普通章节质量债不升级为全局重规划。关闭自动审校也必须经过同一连续性边界，但不生成审校通过证据。

C2 验证：服务端 TypeScript 检查通过；章节运行、资产检查点、自动处理额度、流水线状态与恢复回归 57 项通过。覆盖运行中不缓存成功、同步失败显式返回、旧正文版本拒绝写入、状态发布晚于资产同步、关闭自动审校仍检查连续性，以及明确降级继续生产。未运行收费模型、浏览器、数据库迁移或用户数据写入；A.1 两项遗留继续按用户要求暂缓。

#### C3：抽取与应用可恢复

- [x] 先保存版本化结构抽取结果，再向摘要、事实、资源、伏笔等消费者应用。
- [x] 每个消费者声明幂等身份和应用检查点；同数据库内能原子提交的写入使用短事务。
- [x] 不把模型调用放进数据库长事务；跨消费者使用可恢复的逐项提交。
- [x] 部分应用失败后复用已保存抽取结果，只补失败消费者；旧正文版本的延迟结果不得覆盖新事实。
- [x] 在最终写入点检查预期正文版本和现有保护约束，覆盖用户编辑与后台结果交错的场景。

验收：应用阶段故障不重复调用抽取模型；重复恢复不产生重复事实；过期结果被拒绝或保留为历史。

C3 实施记录：`artifact_delta` 将结构化抽取结果按正文版本保存为独立检查点，再以摘要/事实、状态快照、角色资源、伏笔、角色动态、知识边界、角色思维和对话影响等消费者检查点逐项应用。模型调用不进入数据库事务；任一消费者中断后，恢复只读取既有抽取结果并补做未完成消费者。消费者成功记录保留结果元数据，幂等写入复用稳定键或按来源章节替换；严格伏笔对账即使已完成增量抽取仍会单独补跑。每个最终写入事务均校验当前正文版本，正文在后台过程中变化时以 `failed` 收束，而非以 `degraded` 或 `approved` 掩盖。

C3 验证：服务端 TypeScript 检查通过；小说生产、流水线状态与运行恢复回归 61 项通过。覆盖抽取失败后的复用、单消费者补偿、运行中正文变更拒绝、已完成增量抽取后的严格伏笔对账、pending 不发布完成状态与受保护的基础资产写入。未运行收费模型、浏览器、数据库迁移或用户数据写入；A.1 两项遗留继续按用户要求暂缓。

### 阶段 D：让已有步骤体系具备真实执行能力

范围：R9。仅迁移章节生产这一个连贯子系统。

#### D1：章节执行交接事实

- [x] 将“创建正文流 / 创建修复流 / 创建或复用流水线任务”表示为执行中检查点，而不是章节已经完成。
- [x] 为阶段结果增加统一执行记录，携带目标章节或流水线任务、当前稳定正文版本是否已产生、恢复入口和自动处理额度所有者。
- [x] 移除阶段 Runner 对 `quality_repair` 的机械后跳；质量闭环继续由统一章节运行时在正文完成边界处理。

D1 实施记录：生产阶段的同步返回只说明“已经交接给哪一个执行能力”，不能替代章节完成事实。手动正文/修复流返回 `awaiting_stream_completion`，尚未生成稳定正文版本；流水线任务返回 `queued / running` 以及其唯一 `resume_pipeline_job` 入口和额度所有者。调用方仍可取得原有 stream 或 job payload，保持控制入口兼容；阶段完成、质量债和恢复投影继续读取章节运行时和持久流水线的真实结果，不从本次交接推断。

D1 验证：服务端 TypeScript 检查通过；阶段执行契约、既有手动章节入口和流水线恢复定向回归 11 项通过。未运行收费模型、浏览器、数据库迁移或用户数据写入。

- [x] 列出实际执行职责与事实投影职责，标清每个步骤的真实所有者。
- [x] 基于现有 StepModule 扩展执行输入、产物版本、恢复规则和预算信息，不新增平行引擎。
- [x] 将生成、验收、候选修复、终态提交逐步从隐式循环调用变成清晰的应用能力。
- [x] 外层导演负责任务目标、范围和调度，章节生产负责本章推进；外层不再另行解释同一质量结果。
- [x] 状态投影读取步骤结果，避免以“某类产物存在”代替“当前章当前版本已完成”。
- [x] 手动与自动入口继续复用执行能力，各自保留合法的用户交互差异。

验收：一个仅使用模拟数据的可选步骤能经注册接入、失败恢复并正确投影，无需给主循环添加专用业务分支；已有暂停与恢复行为保持兼容。

D2 实施记录：章节生产拥有生成、验收、候选修复、终态资产与状态提交的执行事实；导演只拥有任务范围、调度与读取这些事实的投影。同步服务为当前正文版本写入独立的连续性边界，导演投影只在该边界、状态提交和可审阅/已批准事实同时存在时才把章节计入完成范围；草稿存在、旧版 `approved` 或其他非当前版本产物均不能冒充完成。正文版本 hash 与连续性边界由 `runtime/artifactSync/` 的稳定契约提供，导演不反向依赖后台同步实现。手动流、修复流和批量流水线继续经过同一章节生产能力，差别只保留在各自的用户交互与恢复入口。

D2 验证：服务端 TypeScript 检查通过；小说生产、章节运行、导演步骤、流水线状态、运行恢复和手动入口回归 108 项通过。覆盖旧版本 approved 不可计入当前完成、当前版本完成/降级边界可计入、范围外章节不影响导演完成比例、运行中检查点不写成功缓存、完成边界写入失败可恢复收束以及恢复只补未完成资产。未运行收费模型、浏览器、数据库迁移或用户数据写入；A.1 两项遗留继续按用户要求暂缓。

D2 闭合复审修复：同正文版本的并发同步将抽取、连续性边界持久化和本地成功缓存收进同一个共享结果，边界写入失败时所有等待者都得到失败，不能有人先看到虚假的 completed。导演的完成判定、进度比例和完成准则统一使用“当前版本资产边界 + 状态提交 + 可审阅/已批准”的闭合事实。流水线跳过策略也改为同一当前版本边界：旧 `approved` / `completed` 记录会重新进入保留正文的收尾恢复，只有边界匹配的终态章节才跳过；恢复期间发现范围已经全部闭合时，任务直接正常完成而不再误报章节不存在。

D2 闭合复审验证：服务端 TypeScript 检查通过；章节生产、章节运行、流水线状态/恢复和导演步骤聚焦回归 96 项通过。新增覆盖并发等待者与边界写入失败的一致结果、缺少边界时导演进度不完成、旧终态章节重新进入恢复、匹配当前边界才允许跳过以及已闭合范围的正常收尾。未运行收费模型、浏览器、数据库迁移或用户数据写入；A.1 两项遗留继续按用户要求暂缓。

### 阶段 E：规划准备与上下文读取分离

范围：R10、R11，并核对规划复用条件。

范围说明：本阶段只处理现有小说生产链路的准备、上下文与失效边界；开发者对本项目提出的新功能设想应单独评审和排期，不作为小说运行时需要理解或重算的“用户创意”。

- [x] 将 JIT 规划/确保计划等写操作移到显式准备步骤；上下文装配只读取已准备产物。
- [x] 统一手动与自动合同的结构校验，区分“结构完整”与“与当前创作要求兼容”。
- [x] 定义上下文提供者合同：适用阶段、输入依赖、优先级、required、token 预算和失败语义。
- [x] 优先迁移角色硬事实、上一章实际结果、章节义务三个必需提供者，再迁移增强信息。
- [x] 必需上下文不得被软信息裁掉；缺失时返回明确缺口，不以空数组伪装完整。
- [x] 在章节边界检查稳定配置和规划版本；验证 API 修改后 Worker 能及时失效缓存。

验收：新增一个上下文提供者无需改动章节主循环；纯上下文读取不触发模型生成或规划写入；关键变更能够正确失效相关合同、上下文和验收缓存。

E1 实施记录：新增章节执行准备边界，统一在正文流或批量章节进入上下文装配前完成 autopilot 路线、执行合同和章节计划准备。上下文装配改为只读取当前章节、既有章节计划与上下文资产；审阅和修复直接读取上下文时不会隐式生成计划或写入卷规划。准备失败在上下文装配和 writer 调用之前显式终止，不能以缺失上下文继续生成。

E1 验证：服务端 TypeScript 检查通过；准备服务、JIT 计划、上下文装配、章节协调器、章节运行、流水线状态与恢复回归 103 项通过。覆盖 autopilot 准备顺序、手动入口不启动路线窗口、装配只读、准备失败不进入装配或 writer，以及既有审阅、修复、质量闭环和任务恢复行为。未运行收费模型、浏览器、数据库迁移或用户数据写入。

E2 实施记录：章节执行合同以当前卷规划中的章节要求作为唯一结构基线，再检查已同步到章节正文入口的目标字数、冲突/揭露强度、禁止事项、任务单和场景卡是否仍与该基线一致。结构完整与版本兼容分别返回判断；只有两者同时满足才复用。手动入口和 autopilot JIT 不再维护各自的字段条件，JIT 统一委托合同服务判断复用或刷新，卷规划调整后不会继续使用表面完整的旧合同。

E3 实施记录：新增章节上下文提供者合同，声明适用阶段、输入依赖、优先级、上下文层级、必需性、Token 预算和失败语义。角色硬事实、上一章实际结果和章节义务作为必需提供者；首章没有上一章结果时不适用，后续章节缺失时返回明确缺口。世界切片和知识库上下文按降级或可选语义记录，不以空数组伪装必需上下文已满足。

E4 实施记录：稳定层缓存新增统一的小说更新失效事件。小说基础设置和写作平台 API 修改后立即失效当前进程的小说快照；章节边界下一次读取会重新加载配置，避免 Worker 继续使用旧的写法、节奏、情绪或平台上下文。既有角色、卷规划、大纲、书级合同和流水线完成事件继续沿用各自的失效路径。

E3/E4 收口记录：三类必需上下文已纳入提供者合同判断，缺失时写入可诊断的上下文缺口；上下文预算选择器本身始终先保留 `required` 块，软信息只能在必需块之后被摘要或裁剪。稳定配置写入口统一通过事件失效缓存，下一章边界重新加载最新上下文。

### 阶段 F：事实来源收敛与质量评测

范围：R12 及长期质量证据不足。

- [ ] 将角色真实事件归属纳入统一结构化资产抽取，保留证据和来源章节。
- [ ] 姓名匹配仅作候选定位，不直接写成确定事实；兼容旧时间线，避免批量覆盖历史小说。
- [ ] 建立小型固定样本：修复倒退、局部义务缺口、事实未提交、角色信息差、修改规划、目标终章收束。
- [ ] 正常 CI 使用保存的结构化输出回放；真实模型只用于有限人工发起的抽样评测。
- [ ] 记录每章调用次数、token、耗时、有效修复率、重复故障、质量债和全书完成结果。
- [ ] 检查当前终章完成是否有真实证据，避免只依据章节数量或 UI 标签；先验证再决定是否扩展结局合同。

验收：能够比较同一批样本改造前后的可靠性与成本；“完成小说”与“质量改善”分别报告，不用自评分上升代替完整结局验收。

## 5. 实施顺序与交付门槛

顺序：A → B → C1 → C2 → C3 → D → E → F。

第一批只做 A、B、C。可靠性未验证前，不同时展开步骤重构和上下文重构。

每个阶段完成时提交以下证据：

1. 变更范围与责任边界。
2. 对应行为测试及结果，明确跳过的检查。
3. 暂停、恢复、旧任务兼容和正文保护是否受影响。
4. 适用的模块 README/wiki 更新与残余风险。
5. 可独立审阅的本地提交；beta 集成和 main 发布按仓库流程另行执行。

## 6. 额度与验证成本控制

- 开发会话默认单代理，先定位再精读，避免重复全仓库扫描。每次只处理一个可验收阶段。
- 本文作为续接入口，后续会话先读对应阶段和已完成证据，不重新做完整架构审查。
- Codex 账户额度与产品调用模型的费用分别管理；本计划不预估无法证实的剩余额度或 token 节省比例。
- 回归验证优先使用模拟依赖、源码行为测试和历史输出回放，不依赖收费 API。
- 真实模型抽样前确定样本、模型、最大请求数与输出上限；无明确预算时不运行批量生成评测。
- 默认不跑全套构建、打包和浏览器验收；跨模块合同变化运行最窄充分检查，UI 验收交由用户。
- 复用验证需确认代码、分支范围和运行环境未变化；旧 dist 不作为当前源码验证依据。
- 每阶段只报告关键结果、验证和剩余风险，不反复输出整份计划。

## 7. 数据、分支和文档治理

- 实施运行时改造前建立 `codex/` 功能分支；遵循功能分支 → beta → main，不直接在 main 开发。
- 新增持久化结构优先增量兼容；任何破坏性操作必须有用户批准、具体备份路径及恢复验证。
- 不清库、不重置小说、不通过批量回写修正状态来掩盖运行时问题。
- 本文件属于待实施开发计划，无用户可见功能变化，因此不更新发布说明和 README 最新更新。
- 本阶段尚未确立新的架构事实，暂不将建议写入 wiki；各实施阶段验证后，把稳定规则写入对应 wiki，计划保留范围和验收进度。
- 生产链、导演恢复、Prompt 上下文及事实资产规则分别维护到现有工作流/架构 wiki，避免复制整份计划作为变更历史。

## 8. 后续执行入口

从阶段 C2“区分同步结果与完成状态”继续，先读取 C1 的选中版本和单次终态提交合同，再收敛资产同步的可判别结果与恢复边界。A.1 两项遗留继续暂缓。每阶段以验收证据为完成标准，不以文件拆分数量或测试总数为标准。

参考：

- [章节生产链路](../wiki/workflows/chapter-production-chain.md)
- [自动导演运行时](../wiki/workflows/auto-director-runtime.md)
- [生产执行边界](../../server/src/services/novel/production/README.md)
- [章节执行协调器](../../server/src/services/novel/runtime/ChapterRuntimeCoordinator.ts)
