# Frontend optional overrides
# 大多数本地开发场景不需要这个文件。
# Release 1 的开发服务和 API 都只接受本机回环访问。
# 不显式配置时，浏览器通过 Vite 的同源 /api 代理访问本机服务。

# 只有在你明确要固定 API 地址时才取消注释：
# VITE_API_BASE_URL=http://127.0.0.1:3000/api
# 默认 10 分钟，可按需要调大。
VITE_API_TIMEOUT_MS=600000
